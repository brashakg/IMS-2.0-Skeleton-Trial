import React from 'react';
import { Routes, Route } from 'react-router-dom';

// TODO: Import Setup screens

const SetupRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div className="p-8">Setup & Configuration Module - Coming Soon</div>} />
    </Routes>
  );
};

export default SetupRoutes;
