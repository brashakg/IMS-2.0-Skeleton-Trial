import React from 'react';
import { Routes, Route } from 'react-router-dom';

// TODO: Import Marketplace screens

const MarketplaceRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div className="p-8">Marketplace Integration Module - Coming Soon</div>} />
    </Routes>
  );
};

export default MarketplaceRoutes;
